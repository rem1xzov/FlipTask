# 🚀 Быстрый старт: Публикация на GitHub

## Шаг 1: Проверка перед публикацией

```bash
# Проверьте, что нет файлов с секретами
git status

# Убедитесь, что .gitignore работает
git check-ignore -v appsettings.Development.json
```

## Шаг 2: Инициализация Git (если еще не сделано)

```bash
cd C:\Users\bkrav\RiderProjects\To-Do_Flip
git init
```

## Шаг 3: Добавление файлов

```bash
# Добавьте все файлы (gitignore автоматически исключит ненужные)
git add .

# Проверьте, что добавлено (не должно быть bin/, obj/, паролей)
git status
```

## Шаг 4: Первый коммит

```bash
git commit -m "Initial commit: FlipTask - система управления задачами

- RESTful API на .NET 9.0
- PostgreSQL база данных
- Docker поддержка
- Красивый фронтенд с анимациями
- Clean Architecture"
```

## Шаг 5: Создание репозитория на GitHub

1. Перейдите на https://github.com/new
2. Создайте новый репозиторий (например, `To-Do_Flip`)
3. **НЕ** инициализируйте с README, .gitignore или лицензией (у нас уже есть)

## Шаг 6: Подключение и публикация

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/To-Do_Flip.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Запушьте код
git push -u origin main
```

## ✅ Готово!

Ваш проект теперь на GitHub! 🎉

## 📝 Что дальше?

1. Обновите README.md - замените `ваш-username` на ваш реальный username
2. Добавьте описание репозитория на GitHub
3. Добавьте теги (tags) для версий
4. Настройте GitHub Actions для CI/CD (опционально)

## ⚠️ Важно помнить

- ✅ `appsettings.json` - можно публиковать (пароль заменен на placeholder)
- ❌ `appsettings.Development.json` - НЕ публиковать (игнорируется)
- ✅ Все `.cs` файлы - можно публиковать
- ❌ Все `bin/` и `obj/` - НЕ публиковать (игнорируются)

---

Подробнее см. [GITHUB_GUIDE.md](./GITHUB_GUIDE.md)
