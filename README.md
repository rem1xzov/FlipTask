# 📝 ConexyTask - Система управления задачами

Современное веб-приложение для управления задачами с красивым интерфейсом, построенное на .NET 9.0 и PostgreSQL.

![.NET](https://img.shields.io/badge/.NET-9.0-512BD4?logo=dotnet)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-316192?logo=postgresql)
![Docker](https://img.shields.io/badge/Docker-Ready-2496ED?logo=docker)

## ✨ Особенности

- 🎨 **Красивый современный UI** с плавными анимациями
- 🚀 **RESTful API** на .NET 9.0
- 🐳 **Docker поддержка** - готово к развертыванию
- 💾 **PostgreSQL** база данных
- 📱 **Адаптивный дизайн** - работает на всех устройствах
- ⚡ **Быстрая работа** - оптимизированная архитектура
- 🔄 **Автоматические миграции** базы данных

## 🏗️ Архитектура проекта

Проект использует Clean Architecture с разделением на слои, аналогично структуре [Flip](https://github.com/rem1xzov/Conexy):

```
To-Do_Flip/
├── 📂 To-Do_Flip/                    # 🌐 Web API (Presentation Layer)
│   ├── Controllers/                  # API контроллеры
│   ├── Contract/                     # DTOs (Request/Response)
│   └── Program.cs                    # Точка входа
│
├── 📂 To-Do_Flip.Application/         # 🧠 Application Layer
│   ├── Service/                      # Бизнес-логика
│   └── Repository/                   # Репозитории
│
├── 📂 To-Do_Flip.DataAccess/          # 🗄️ Data Access Layer (ВСЕ БД-СВЯЗАННОЕ)
│   ├── Entity/                       # Entity модели (таблицы БД)
│   ├── DbContext/                    # Контекст БД
│   ├── Configuration/                # Конфигурации EF
│   └── Migrations/                   # Миграции БД
│
└── 📂 To-Do_Flip.Store/               # 📦 Domain Layer (МОДЕЛИ)
    └── Model/                         # Доменные модели
```

📚 **Подробная организация проекта:** [ORGANIZATION.md](./ORGANIZATION.md)

### 🗄️ Все БД-связанное в одном месте
**Расположение:** `To-Do_Flip.DataAccess/`
- Entity модели → `Entity/`
- Контекст БД → `DbContext/`
- Конфигурации → `Configuration/`
- Миграции → `Migrations/`

### 📦 Все модели в одном месте
- Доменные модели → `To-Do_Flip.Store/Model/`
- Entity модели (для БД) → `To-Do_Flip.DataAccess/Entity/`

## 🚀 Быстрый старт

### Предварительные требования

- [.NET 9.0 SDK](https://dotnet.microsoft.com/download/dotnet/9.0)
- [PostgreSQL](https://www.postgresql.org/download/) (или Docker)
- [Docker Desktop](https://www.docker.com/products/docker-desktop) (опционально)

### Локальная разработка

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/rem1xzov/To-Do_Flip.git
   cd To-Do_Flip
   ```

2. **Настройте базу данных**
   
   Создайте файл `To-Do_Flip/appsettings.Development.json`:
   ```json
   {
     "ConnectionStrings": {
       "DefaultConnection": "Host=localhost;Port=5432;Database=To_Do_Flip;Username=postgres;Password=******"
     }
   }
   ```

3. **Примените миграции**
   ```bash
   cd To-Do_Flip.DataAccess
   dotnet ef database update --startup-project ..\To-Do_Flip\To-Do_Flip.csproj --context To_Do_Flip_Context
   ```

4. **Запустите приложение**
   ```bash
   cd To-Do_Flip
   dotnet run
   ```

   API будет доступен на `http://localhost:5221`

### Запуск через Docker

1. **Соберите и запустите контейнеры**
   ```bash
   docker-compose up -d --build
   ```

2. **Проверьте статус**
   ```bash
   docker-compose ps
   ```

3. **API будет доступен на** `http://localhost:8080`

Подробнее о Docker в [DOCKER_README.md](./DOCKER_README.md)

### Деплой на Render (из GitHub)

Render **не запускает** `docker-compose.yml`, когда ты подключаешь репозиторий как один Web Service. Поэтому Postgres из compose не поднимется, и приложение попытается подключиться к `localhost:5432` (что внутри контейнера не является БД).

Чтобы деплой работал “в один клик”, в репозитории есть `render.yaml` (Blueprint), который создаёт:
- **Web Service** (API из `Dockerfile`)
- **Managed PostgreSQL** (БД в Render)

**Как развернуть:**
1. Открой Render → **New** → **Blueprint**
2. Выбери репозиторий
3. Render сам создаст сервисы по `render.yaml`

**Важно:**
- Render задаёт порт через переменную окружения `PORT` — это поддержано в `To-Do_Flip/Program.cs`
- Render Postgres часто отдаёт connection string в формате `postgres://...` — мы автоматически конвертируем её в формат Npgsql в `To-Do_Flip/Program.cs`

## 📡 API Endpoints

### Получить все задачи
```http
GET /api/tasks
```

**Ответ:**
```json
[
  {
    "id": "guid",
    "name": "Название задачи",
    "description": "Описание задачи"
  }
]
```

### Создать задачу
```http
POST /api/tasks
Content-Type: application/json

{
  "name": "Название задачи",
  "description": "Описание задачи"
}
```

**Ответ:** `200 OK`
```json
{
  "id": "guid",
  "name": "Название задачи",
  "description": "Описание задачи"
}
```

### Обновить задачу
```http
PUT /api/tasks/{id}
Content-Type: application/json

{
  "name": "Обновленное название",
  "description": "Обновленное описание"
}
```

**Ответ:** `200 OK` (возвращает Guid задачи)

### Удалить задачу
```http
DELETE /api/tasks/{id}
```

**Ответ:** `200 OK` (возвращает Guid удаленной задачи)

## 🎨 Фронтенд

Фронтенд находится в отдельном файле `FlipTask.html`. Это одностраничное приложение с современным дизайном.

### Использование

1. Откройте `ConexyTask.html` в браузере
2. По умолчанию подключение к `http://localhost:8080` (Docker)
3. Для локальной разработки используйте: `ConexyTask.html?api=http://localhost:5221`

## 🛠️ Разработка

### Структура проекта

- **To-Do_Flip** - Web API проект
- **To-Do_Flip.Application** - Бизнес-логика и сервисы
- **To-Do_Flip.DataAccess** - Работа с базой данных
- **To-Do_Flip.Store** - Доменные модели

### Работа с миграциями

Создание новой миграции:
```bash
cd To-Do_Flip.DataAccess
dotnet ef migrations add ИмяМиграции --startup-project ..\To-Do_Flip\To-Do_Flip.csproj --context To_Do_Flip_Context
```

Применение миграций:
```bash
dotnet ef database update --startup-project ..\To-Do_Flip\To-Do_Flip.csproj --context To_Do_Flip_Context
```

Подробнее в [MIGRATIONS_COMMANDS.md](./To-Do_Flip.DataAccess/MIGRATIONS_COMMANDS.md)

### Технологии

- **Backend:** .NET 9.0, ASP.NET Core Web API
- **Database:** PostgreSQL, Entity Framework Core
- **Frontend:** HTML5, CSS3, JavaScript (Vanilla)
- **Containerization:** Docker, Docker Compose
- **Architecture:** Clean Architecture, Repository Pattern

## 📦 Зависимости

### Backend
- `Microsoft.AspNetCore.OpenApi` (9.0.12)
- `Microsoft.EntityFrameworkCore.Design` (9.0.2)
- `Npgsql.EntityFrameworkCore.PostgreSQL` (для PostgreSQL)

## 🔒 Безопасность

⚠️ **Важно:** Не коммитьте файлы с паролями и секретами!

- Используйте `appsettings.Development.json` для локальной разработки
- Используйте переменные окружения для продакшена
- Файл `appsettings.json` содержит только примеры без реальных паролей

## 🐳 Docker

Проект полностью готов к развертыванию через Docker:

- **PostgreSQL** контейнер с персистентным хранилищем
- **API** контейнер с автоматическим применением миграций
- **Health checks** для надежности
- **Networking** настроен автоматически

См. [DOCKER_README.md](./DOCKER_README.md) для подробностей.

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.


## 🙏 Благодарности

- .NET команда за отличный фреймворк
- Сообщество разработчиков за вдохновение


⭐ Если проект вам понравился, поставьте звезду!
