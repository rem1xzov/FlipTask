# 📋 Руководство по публикации на GitHub

## ✅ Файлы, которые МОЖНО публиковать

### Исходный код
- ✅ Все `.cs` файлы (C# код)
- ✅ Все `.csproj` файлы (проекты)
- ✅ `.sln` файл (solution файл)
- ✅ `Dockerfile`
- ✅ `docker-compose.yml`
- ✅ `.dockerignore`

### Конфигурация
- ✅ `appsettings.json` (без паролей)
- ✅ `appsettings.Example.json` (пример конфигурации)
- ✅ `README.md`
- ✅ `DOCKER_README.md`
- ✅ `MIGRATIONS_COMMANDS.md`
- ✅ `GITHUB_GUIDE.md` (этот файл)

### Документация
- ✅ Все `.md` файлы
- ✅ Комментарии в коде

### Фронтенд
- ✅ `FlipTask.html` (если находится в репозитории)

## ❌ Файлы, которые НЕЛЬЗЯ публиковать

### Скомпилированные файлы
- ❌ `bin/` - все папки bin
- ❌ `obj/` - все папки obj
- ❌ `*.dll` - скомпилированные библиотеки
- ❌ `*.pdb` - файлы отладки
- ❌ `*.exe` - исполняемые файлы

### Конфигурация с секретами
- ❌ `appsettings.Development.json` (может содержать пароли)
- ❌ `appsettings.Production.json` (может содержать пароли)
- ❌ `.env` файлы
- ❌ Файлы с реальными паролями и ключами

### IDE файлы
- ❌ `.vs/` - Visual Studio настройки
- ❌ `.vscode/` - VS Code настройки (кроме общих настроек)
- ❌ `.idea/` - JetBrains Rider настройки
- ❌ `*.suo` - Visual Studio пользовательские настройки
- ❌ `*.user` - пользовательские файлы проектов
- ❌ `rider.project.model.nuget.info`
- ❌ `rider.project.restore.info`

### NuGet кэш
- ❌ `packages/` - папка с пакетами NuGet
- ❌ `project.assets.json`
- ❌ `project.nuget.cache`
- ❌ `*.nupkg` - пакеты NuGet
- ❌ `*.csproj.nuget.*` - временные файлы NuGet

### База данных
- ❌ `*.db` - файлы базы данных
- ❌ `*.sqlite` - SQLite файлы
- ❌ `*.sqlite3` - SQLite файлы

### Логи и временные файлы
- ❌ `logs/` - папка с логами
- ❌ `*.log` - файлы логов
- ❌ `*.tmp` - временные файлы
- ❌ `*.temp` - временные файлы

### OS файлы
- ❌ `.DS_Store` - macOS
- ❌ `Thumbs.db` - Windows
- ❌ `Desktop.ini` - Windows

## 🔍 Как проверить перед коммитом

### 1. Проверьте .gitignore
Убедитесь, что `.gitignore` файл присутствует и содержит все необходимые исключения.

### 2. Проверьте статус Git
```bash
git status
```

Убедитесь, что в списке нет:
- Файлов из папок `bin/` и `obj/`
- Файлов с паролями
- IDE файлов

### 3. Проверьте appsettings.json
Убедитесь, что в `appsettings.json` нет реальных паролей:
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=To_Do_Flip;Username=postgres;Password=123456"
  }
}
```

Если есть реальные пароли, замените их на `YOUR_PASSWORD_HERE` или используйте переменные окружения.

### 4. Используйте appsettings.Example.json
Создайте `appsettings.Example.json` с примером конфигурации без паролей и добавьте его в репозиторий.

## 📝 Рекомендуемая структура коммита

```bash
# 1. Добавьте все файлы
git add .

# 2. Проверьте, что добавлено
git status

# 3. Если все правильно, сделайте коммит
git commit -m "Initial commit: FlipTask - система управления задачами"

# 4. Добавьте remote (замените на ваш URL)
git remote add origin https://github.com/ваш-username/To-Do_Flip.git

# 5. Запушьте
git push -u origin main
```

## 🚨 Если случайно закоммитили секреты

Если вы случайно закоммитили файлы с паролями или секретами:

1. **Немедленно** измените пароли/ключи в реальной системе
2. Удалите секреты из истории Git:
   ```bash
   git filter-branch --force --index-filter \
     "git rm --cached --ignore-unmatch путь/к/файлу/с/секретом" \
     --prune-empty --tag-name-filter cat -- --all
   ```
3. Или используйте [BFG Repo-Cleaner](https://rtyley.github.io/bfg-repo-cleaner/)

## 📌 Чеклист перед публикацией

- [ ] `.gitignore` файл создан и настроен
- [ ] Все `bin/` и `obj/` папки исключены
- [ ] Нет реальных паролей в `appsettings.json`
- [ ] Создан `appsettings.Example.json`
- [ ] `README.md` заполнен и актуален
- [ ] Все IDE файлы исключены
- [ ] Проверен `git status` - нет лишних файлов
- [ ] Код закомментирован где необходимо
- [ ] Лицензия указана (если нужно)

## 💡 Дополнительные рекомендации

1. **Используйте переменные окружения** для секретов в продакшене
2. **Создайте шаблон** для новых разработчиков
3. **Добавьте LICENSE** файл
4. **Настройте GitHub Actions** для CI/CD (опционально)
5. **Добавьте badges** в README для статуса сборки

---

Удачи с публикацией! 🚀
