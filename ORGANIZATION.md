# 📂 Организация проекта FlipTask

Структура проекта организована по принципу Clean Architecture, аналогично проекту [Flip](https://github.com/rem1xzov/Flip).

## 🎯 Структура проекта

```
To-Do_Flip/
│
├── 📂 To-Do_Flip/                    # 🌐 Web API (Presentation Layer)
│   ├── Controllers/                  # API контроллеры
│   ├── Contract/                     # DTOs (Request/Response)
│   ├── Properties/                   # Настройки
│   ├── Program.cs                    # Точка входа
│   └── appsettings.json              # Конфигурация
│
├── 📂 To-Do_Flip.Application/         # 🧠 Application Layer
│   ├── Service/                      # Бизнес-логика
│   └── Repository/                   # Репозитории
│
├── 📂 To-Do_Flip.DataAccess/          # 🗄️ Data Access Layer (ВСЕ БД-СВЯЗАННОЕ)
│   ├── Entity/                       # Entity модели (таблицы БД)
│   ├── DbContext/                    # Контекст БД
│   ├── Configuration/                # Конфигурации EF
│   └── Migrations/                   # Миграции БД
│
└── 📂 To-Do_Flip.Store/               # 📦 Domain Layer (МОДЕЛИ)
    └── Model/                         # Доменные модели
```

## 📊 Детальная структура по папкам

### 🌐 To-Do_Flip (Web API)

**Назначение:** Слой представления, API endpoints

| Папка | Содержимое |
|-------|-----------|
| `Controllers/` | API контроллеры для обработки HTTP запросов |
| `Contract/` | DTOs (Data Transfer Objects) для запросов и ответов |
| `Properties/` | Настройки запуска приложения |

**Файлы:**
- `Program.cs` - Точка входа, настройка приложения
- `appsettings.json` - Конфигурация (без паролей)
- `appsettings.Development.json` - Конфигурация для разработки

### 🧠 To-Do_Flip.Application

**Назначение:** Бизнес-логика и сервисы

| Папка | Содержимое |
|-------|-----------|
| `Service/` | Бизнес-логика, интерфейсы и реализации сервисов |
| `Repository/` | Интерфейсы и реализации репозиториев для работы с данными |

### 🗄️ To-Do_Flip.DataAccess (ВСЕ БД-СВЯЗАННОЕ)

**Назначение:** Работа с базой данных

| Папка | Содержимое |
|-------|-----------|
| `Entity/` | Entity Framework модели (представляют таблицы БД) |
| `DbContext/` | Контекст Entity Framework для работы с БД |
| `Configuration/` | Конфигурации Entity Framework (настройки полей, ограничений) |
| `Migrations/` | Миграции базы данных (создание и обновление структуры) |

**Файлы:**
- `Entity/To-Do_Flip_Entity.cs` - Модель таблицы в БД
- `DbContext/To_Do_Flip_Context.cs` - Контекст БД
- `Configuration/To_Do_Flip_Configuration.cs` - Настройки EF
- `Migrations/*.cs` - Файлы миграций

### 📦 To-Do_Flip.Store (МОДЕЛИ)

**Назначение:** Доменные модели

| Папка | Содержимое |
|-------|-----------|
| `Model/` | Доменные модели с бизнес-логикой |

**Файлы:**
- `Model/To_Do.cs` - Доменная модель задачи

## 🔍 Быстрая навигация

### Ищу API контроллеры?
→ `To-Do_Flip/Controllers/`

### Ищу DTOs?
→ `To-Do_Flip/Contract/`

### Ищу бизнес-логику?
→ `To-Do_Flip.Application/Service/`

### Ищу репозитории?
→ `To-Do_Flip.Application/Repository/`

### Ищу ВСЕ БД-связанное?
→ `To-Do_Flip.DataAccess/`
- Entity модели → `Entity/`
- Контекст БД → `DbContext/`
- Конфигурации → `Configuration/`
- Миграции → `Migrations/`

### Ищу доменные модели?
→ `To-Do_Flip.Store/Model/`

## 📚 README файлы

В каждой папке есть README.md для навигации:
- `To-Do_Flip/README.md`
- `To-Do_Flip.Application/README.md`
- `To-Do_Flip.DataAccess/README.md`
- `To-Do_Flip.Store/README.md`

## 🎨 Сравнение с Flip

Структура организована аналогично проекту [Flip](https://github.com/rem1xzov/Flip):

**Flip:**
```
FlipChatAnon/
├── Controllers/
├── Hubs/
├── Middleware/
├── FlipStoreAnon.DbContext/
└── FlipChatStore/
```

**FlipTask:**
```
To-Do_Flip/
├── To-Do_Flip/Controllers/
├── To-Do_Flip.DataAccess/ (DbContext, Entity, Configuration, Migrations)
└── To-Do_Flip.Store/ (Models)
```

## ✅ Преимущества структуры

- ✅ Все БД-связанное в одном месте (`DataAccess`)
- ✅ Все модели в одном месте (`Store`)
- ✅ Четкое разделение ответственности
- ✅ Легкая навигация по папкам
- ✅ README в каждой папке для справки

---

**Последнее обновление:** 2025-02-01
